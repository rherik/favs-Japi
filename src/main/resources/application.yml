spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/rest_spring
    username: postgres
    password: "123"
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: false